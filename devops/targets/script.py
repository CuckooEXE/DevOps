"""Script — target that runs commands but produces no tracked output."""

# Re-exported from core.target so the Script class lives in one place.
from devops.core.target import Script

__all__ = ["Script"]
